/**
 * ============================================================
 * APP ENTRY POINT — Initialize the YouTube Playlist Manager
 * ============================================================
 * This is the main entry script. It waits for the DOM to load
 * and then initializes the UI module which handles everything.
 */

document.addEventListener('DOMContentLoaded', () => {
  console.log('🎬 YouTube Playlist Manager — Initializing...');
  UI.init();
});
